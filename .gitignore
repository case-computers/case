# Python
.venv
__pycache__/
*.py[cod]
.pytest_cache/

# OS
.DS_Store

# Local runtime state (the vault key + case.db live in ~/.case, never here — belt & braces)
*.db
*.key
*.log
.case/

# Operator deploy (non-secret knobs only — Hetzner token lives in hcloud CLI context)
.env.operator.local
.env
*.env.local

# Generated partner client bundles (may contain IP + connect notes)
deploy/bundles/

# Operator fleet snapshots belong under ~/.case/ops/ — never commit
**/fleet.json
.case/ops/

# Local git worktrees
.worktrees/
docs/

# Local agent handoff notes (never product docs)
context.md

# Drive UI local server
web/node_modules/
# drive v2 local thread store
web/web-ui/threads.json
