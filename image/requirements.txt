# deskd's venv, transitives included: the image is the computer, and a rebuild
# months from now must produce the deskd that was tested. websockify is installed
# on its own with --no-deps (see Dockerfile).
fastapi==0.141.1
uvicorn==0.52.4
websocket-client==1.9.2
requests==2.34.2
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
h11==0.16.0
idna==3.19
pydantic==2.13.5
pydantic-core==2.46.5
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.7.0
