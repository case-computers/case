{
  "name": "case-drive-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node web-ui/serve.mjs",
    "test": "for f in web-ui/test_*.mjs; do node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "openai": "^6.7.0"
  }
}
